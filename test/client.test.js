const assert = require('assert');
const Clients = require('./lib/client');

describe('Clients', () => {
    
    const users = [];
    let clients = null;

    beforeEach(() => {
        clients = new Clients();
        clients.add([]);
    });

    it('assigns a user name', () => {
        assert.equal(this.clients, )
    });

    
});
